name: lib_requests
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.3.0 <3.0.0'

dependencies:
  http: ^0.12.0+1

  lib_model:
    path: ../../libs/lib_model

dev_dependencies:
  pedantic: ^1.9.0
