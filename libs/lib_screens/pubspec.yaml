name: lib_screens
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.3.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  lib_requests:
    path: ../../libs/lib_requests

  lib_bottom_bar:
    path: ../../libs/lib_bottom_bar

  lib_charts:
    path: ../../libs/lib_charts

  lib_ddbb:
    path: ../../libs/lib_ddbb

  lib_model:
    path: ../../libs/lib_model


dev_dependencies:
  pedantic: ^1.9.0
