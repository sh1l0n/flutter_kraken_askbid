name: lib_charts
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.3.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  charts_flutter:
  charts_common:
  
  lib_model:
    path: ../../libs/lib_model
    

dev_dependencies:
  pedantic: ^1.9.0
